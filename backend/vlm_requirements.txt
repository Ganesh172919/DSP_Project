# VLM Dependencies — Install on top of existing requirements.txt
# Usage: pip install -r vlm_requirements.txt
#
# These packages enable Vision Language Model reasoning.
# The system auto-detects hardware and picks the best model:
#   - GPU (CUDA): Qwen2.5-VL-3B-Instruct (4-bit quantized)
#   - CPU / Low RAM: moondream2 (1.9B, fp16)

# ─── Core VLM ──────────────────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.25.0
einops>=0.7.0
psutil>=5.9.0

# ─── Quantization (4-bit support, requires CUDA) ──────────────────────
# bitsandbytes is CUDA-only — skip on CPU-only machines
# Install manually: pip install bitsandbytes>=0.41.0
# On CPU-only systems, moondream2 will run in fp16/fp32 instead.

# ─── Qwen2.5-VL specific ──────────────────────────────────────────────
qwen-vl-utils>=0.0.2
sentencepiece>=0.1.99
protobuf>=3.20.0

# ─── moondream2 specific ──────────────────────────────────────────────
# moondream2 uses timm internally
timm>=0.9.0

# ─── Colab notebook extras ────────────────────────────────────────────
# gradio>=4.0.0  # Only needed for Colab demo notebook
